# Node (klasör ya da kısayol olabilir; sondaki eğik çizgi kısayolu yakalamıyordu)
webapp/node_modules

# Derlenmiş arayüz (CI ve build.sh üretir)
web/dist/*
!web/dist/.gitkeep

# Derlenmiş program ve paketler
/haproxy-lens
/build/
/dist/
*.tar.gz
